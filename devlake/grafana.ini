[security]
# Required for iframe embed from another origin (for example dashboard on :3000).
allow_embedding = true

[auth.anonymous]
enabled = true
org_name = Main Org.
org_role = Viewer

[auth]
# Keep login enabled by default; set true if you only want anonymous access.
disable_login_form = false
